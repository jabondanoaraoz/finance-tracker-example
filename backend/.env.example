# URL del Google Apps Script Web App desplegado como endpoint HTTP.
# Copiar este archivo a backend/.env y pegar la URL real (termina en /exec).
SHEETS_API_URL=https://script.google.com/macros/s/XXXXXXXX/exec
